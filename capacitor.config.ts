import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.2eatFVM.app',
  appName: '2eat',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
